spring.application.name=process-service

spring.mongodb.uri=${SPRING_MONGODB_URI:mongodb://localhost:27017/pension_flow}

server.port=${SERVER_PORT:8080}

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
management.info.env.enabled=true

info.app.name=Pension Flow Process Service
info.app.description=API for administrative pension processes
